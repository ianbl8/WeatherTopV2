<table class="ui fixed table">
  <thead>
  <tr>
    <!-- DATE AND TIME for Release 3
    <th>Date and time</th>
    -->
    <th>Weather code</th>
    <th>Temperature (°C)</th>
    <th>Wind speed (km/h)</th>
    <th>Wind direction (°)</th>
    <th>Pressure (hPa)</th>
  </tr>
  </thead>
  <tbody>
  {{#each station.readings}}
    <tr>
      <!-- DATE AND TIME for Release 3
      <td>(TBC)</td>
      -->
      <td>{{code}}</td>
      <td>{{temperature}}</td>
      <td>{{windSpeed}}</td>
      <td>{{windDirection}}</td>
      <td>{{pressure}}</td>
    </tr>
  {{/each}}
  </tbody>
</table>